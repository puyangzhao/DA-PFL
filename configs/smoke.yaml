data_path: data/nhanes_cleaned.csv
output_dir: results/smoke
tasks: [diabetes]
partitions: [demographic]
random_clients: 10
models: [mlp]
algorithms: [fedavg, fedprox, fedrep, scaffold]
seeds: [42]
test_size: 0.2
rounds: 2
local_epochs: 1
batch_size: 32
learning_rate: 0.01
fedprox_mu: 0.01
early_stopping_patience: 20
early_stopping_min_delta: 0.001
weighted_aggregation: false

